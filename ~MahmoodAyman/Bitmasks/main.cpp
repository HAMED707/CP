#include <bits/stdc++.h>

#define HAMED ios_base::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);

#define txt  freopen("input.txt","r",stdin); freopen("output.txt","w",stdout); freopen("error.txt","w",stderr);


#define ll long long
#define ld long double
#define all(X) X.begin(),X.end()


#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/tree_policy.hpp>
using namespace __gnu_pbds;

#define ordered_set tree<int, null_type,less<int>, rb_tree_tag,tree_order_statistics_node_update>



using namespace std;
const int N=1e5+5;

void solve(){
   __builtin_clz();//count left leading zeros
   __builtin_ctz();//count right leading zeros
    __builtin_popcount();//count num of 1
    // to get subset of sequens using bitmask so i will nove for (2^n) -1
    __lg();//get index the most signifigant bit (msp) // the opesite is (lsp)'least sp'-->(x&-x)
}

int main (){

    //txt
    HAMED
    solve();
    return 0;
}